# account_move_account_move_send_batch_wizard_rel

## Source system
This table originates from an Odoo ERP system. The naming convention `_rel` combined with the specific entity names `account_move` (representing accounting journal entries) and `account_move_send_batch_wizard` (a UI wizard for batch processing documents) is characteristic of Odoo's many-to-many relationship tables generated by the ORM.

## Functional process 
This table supports the "Batch Document Sending" process, where multiple accounting journal entries are grouped together to be sent (e.g., via email or post) through a single wizard execution. It acts as the join table linking specific journal entries to a batch processing session.

## Description
One row in this table represents a single association between an accounting journal entry and a batch sending wizard instance. It is a raw landing copy of an Odoo many-to-many join table, used to maintain the relationship between the batch wizard and the individual moves included in that batch.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| account_move_send_batch_wizard_id | INTEGER | false | Foreign key to the batch wizard instance | Links to the wizard session record. |
| account_move_id | INTEGER | false | Foreign key to the accounting journal entry | Links to the specific invoice or journal entry. |

## Keys

- **Primary key (inferred):** The composite of (`account_move_send_batch_wizard_id`, `account_move_id`).
- **Foreign keys (inferred):** 
    - `account_move_send_batch_wizard_id` → `account_move_send_batch_wizard.id`: This column references the parent wizard record that manages the batch.
    - `account_move_id` → `account_move.id`: This column references the specific journal entry being processed in the batch.
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- This table is a pure join table; it contains no business data other than the relationship identifiers.
- There are no timestamps or audit columns present; rely on the parent tables for creation or modification dates.
- As a standard Odoo `_rel` table, it does not contain a surrogate primary key column (like `id`), so queries should join or filter on the composite key.