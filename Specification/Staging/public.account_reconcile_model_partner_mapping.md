# account_reconcile_model_partner_mapping

## Source system
This table originates from Odoo ERP. The naming convention `account_reconcile_model_...` and the presence of `create_uid`, `write_uid`, and `_seq` sequence defaults are characteristic of the Odoo framework's ORM layer.

## Functional process 
This table supports the automated bank reconciliation process. It defines mapping rules that link specific reconciliation models to business partners based on regex patterns found in payment references or narration fields, facilitating the automated matching of bank statement lines to partner accounts.

## Description
One row in this table represents a specific configuration rule that associates a reconciliation model with a partner based on text pattern matching. It serves as a raw landing copy of the Odoo `account.reconcile.model.partner.mapping` model, capturing the criteria used to automate accounting entries.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| model_id | INTEGER | false | Foreign key to the reconciliation model | Links to `account.reconcile.model`. |
| partner_id | INTEGER | false | Foreign key to the business partner | Links to `res.partner`. |
| create_uid | INTEGER | true | User ID who created the record | References `res.users`. |
| write_uid | INTEGER | true | User ID who last updated the record | References `res.users`. |
| payment_ref_regex | VARCHAR | true | Regex pattern for payment reference | Used to match bank statement labels. |
| narration_regex | VARCHAR | true | Regex pattern for narration | Used to match bank statement notes. |
| create_date | TIMESTAMP | true | Record creation timestamp | UTC assumed. |
| write_date | TIMESTAMP | true | Last update timestamp | UTC assumed. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `model_id` → `account_reconcile_model.id` (Inferred from Odoo naming convention).
    - `partner_id` → `res_partner.id` (Inferred from Odoo naming convention).
    - `create_uid` → `res_users.id` (Standard Odoo audit field).
    - `write_uid` → `res_users.id` (Standard Odoo audit field).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- Timestamps (`create_date`, `write_date`) are stored in UTC as per standard Odoo configuration.
- The `payment_ref_regex` and `narration_regex` columns contain raw regular expression strings; ensure proper escaping if used in downstream SQL pattern matching.
- This table does not implement soft deletes; records are typically removed via hard delete in the source system.
- `VARCHAR` lengths are not explicitly constrained in the metadata; assume variable length based on Odoo's default implementation (often 255 or unlimited).