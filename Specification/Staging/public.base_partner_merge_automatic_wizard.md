# base_partner_merge_automatic_wizard

## Source system
This table originates from Odoo (ERP), as evidenced by the naming convention `base_partner_merge_automatic_wizard`, the use of `create_uid`/`write_uid` audit columns, and the specific sequence-based default value for the `id` column.

## Functional process 
This table supports the automated customer/partner deduplication process within the Odoo CRM/Sales module. It tracks the configuration and progress of wizard-driven merge operations, allowing the system to group duplicate partner records based on criteria like email, VAT, or name, and manage the transition of data from source records to a destination partner.

## Description
One row in this table represents a single execution instance or session of the automated partner merge wizard. It acts as a staging record that stores the user's configuration settings for a merge job and tracks the current state of the operation. This is a raw landing table representing the transient state of an Odoo wizard process.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| number_group | INTEGER | true | Current group index | Tracks the progress of the merge batch. |
| current_line_id | INTEGER | true | Reference to current line | Likely links to a detail table of records being merged. |
| dst_partner_id | INTEGER | true | Destination partner ID | The target partner record that will survive the merge. |
| maximum_group | INTEGER | true | Total groups to process | The upper bound of the merge batch. |
| create_uid | INTEGER | true | Creator user ID | Foreign key to the system user who initiated the wizard. |
| write_uid | INTEGER | true | Last modifier user ID | Foreign key to the system user who last updated the wizard. |
| state | VARCHAR | false | Lifecycle state | Current status of the wizard (e.g., 'option', 'run', 'finished'). |
| group_by_email | BOOLEAN | true | Grouping criteria: Email | Flag indicating if email address is used for matching. |
| group_by_name | BOOLEAN | true | Grouping criteria: Name | Flag indicating if partner name is used for matching. |
| group_by_is_company | BOOLEAN | true | Grouping criteria: Is Company | Flag indicating if company status is used for matching. |
| group_by_vat | BOOLEAN | true | Grouping criteria: VAT | Flag indicating if VAT number is used for matching. |
| group_by_parent_id | BOOLEAN | true | Grouping criteria: Parent ID | Flag indicating if parent partner is used for matching. |
| exclude_contact | BOOLEAN | true | Exclude contact records | Filter flag for the merge operation. |
| exclude_journal_item | BOOLEAN | true | Exclude journal items | Filter flag for the merge operation. |
| create_date | TIMESTAMP | true | Creation timestamp | Timestamp of wizard creation. |
| write_date | TIMESTAMP | true | Last update timestamp | Timestamp of last modification. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `dst_partner_id` → `res_partner.id` (Guess: links to the surviving partner record).
    - `create_uid` → `res_users.id` (Guess: standard Odoo audit link).
    - `write_uid` → `res_users.id` (Guess: standard Odoo audit link).
- **Natural keys (inferred):** Not confidently inferable.

## Caveats for downstream consumers

- **Sensitive Data:** Contains `group_by_email` and `group_by_vat` flags; while the table doesn't contain the PII itself, it defines the logic used to identify and merge records containing PII.
- **Timestamps:** Timestamps are stored in the Odoo application server time, typically UTC.
- **Lifecycle:** This table tracks transient wizard sessions; rows may be ephemeral or represent historical logs of merge operations depending on Odoo's cleanup configuration.
- **Data Quality:** As a wizard state table, many columns may be null depending on the specific stage of the merge process.