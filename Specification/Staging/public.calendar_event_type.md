# calendar_event_type

## Source system
This table originates from an Odoo ERP system. The naming convention of columns such as `create_uid`, `write_uid`, `create_date`, and `write_date`, combined with the use of a sequence-based default for the `id` column, is characteristic of the Odoo ORM framework.

## Functional process 
This table supports the configuration of calendar event categories or types within the scheduling module. It allows users to define distinct labels and visual identifiers (via the `color` column) for different types of meetings or appointments, facilitating organized calendar management.

## Description
One row in this table represents a single definition of a calendar event type (e.g., "Internal Meeting", "Client Call"). This is a raw landed staging table containing metadata for event classification, used to populate dropdowns or filter views in the application's scheduling interface.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| color | INTEGER | true | UI color index | Represents an integer index for color-coding in the frontend. |
| create_uid | INTEGER | true | Creator user ID | Foreign key to the system user who created this record. |
| write_uid | INTEGER | true | Last modifier user ID | Foreign key to the system user who last updated this record. |
| name | VARCHAR | false | Event type label | The display name of the event category. |
| create_date | TIMESTAMP | true | Creation timestamp | Timestamp of record creation; timezone typically UTC. |
| write_date | TIMESTAMP | true | Last update timestamp | Timestamp of the last modification; timezone typically UTC. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Guess: standard Odoo pattern for audit tracking).
    - `write_uid` → `res_users.id` (Guess: standard Odoo pattern for audit tracking).
- **Natural keys (inferred):** 
    - `name` (Assuming event type names are unique within the system).

## Caveats for downstream consumers

- **Timestamps:** Assumed to be in UTC, consistent with standard Odoo database configurations.
- **Soft Deletes:** This table does not appear to implement a soft-delete flag (e.g., `active` column); assume all rows are currently active unless otherwise specified by business logic.
- **Data Integrity:** The `color` column is an integer index; downstream consumers should map these to the specific color palette used by the source application's frontend.
- **Audit Columns:** `create_uid` and `write_uid` are likely references to a `res_users` table; ensure joins are handled if user names are required.