# crm_lead_crm_lead_lost_rel

## Source system
This table likely originates from a CRM system (e.g., Odoo or a similar relational CRM framework). The naming convention `_rel` is a standard pattern for join tables representing many-to-many or one-to-many relationships between