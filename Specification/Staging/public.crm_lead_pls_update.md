# crm_lead_pls_update

## Source system
This table originates from an Odoo ERP system. The naming convention (e.g., `create_uid`, `write_uid`, `create_date`, `write_date`) and the use of `nextval` sequences for primary keys are characteristic of the Odoo framework's ORM layer.

## Functional process 
This table supports the Lead Scoring or Predictive Lead Scoring (PLS) process within the CRM module. It tracks the temporal configuration or status updates for lead scoring models, likely used to determine the start date for lead evaluation periods.

## Description
Each row represents a configuration or update event for a Predictive Lead Scoring (PLS) record. It serves as a raw landed staging table capturing audit metadata and the effective start date for scoring logic.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Uses `crm_lead_pls_update_id_seq` sequence. |
| create_uid | INTEGER | true | ID of the user who created the record | References the `res_users` table. |
| write_uid | INTEGER | true | ID of the user who last updated the record | References the `res_users` table. |
| pls_start_date | DATE | false | The effective start date for the PLS model | Business-critical date for scoring calculations. |
| create_date | TIMESTAMP | true | Timestamp of record creation | In UTC, generated by the application layer. |
| write_date | TIMESTAMP | true | Timestamp of last record update | In UTC, generated by the application layer. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Standard Odoo pattern for audit tracking).
    - `write_uid` → `res_users.id` (Standard Odoo pattern for audit tracking).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **Timestamps:** `create_date` and `write_date` are stored in UTC.
- **Audit columns:** `create_uid` and `write_uid` are nullable; ensure your joins to `res_users` handle potential nulls if the user has been deleted or the record was created via system process.
- **Data Integrity:** As a staging table, this may contain multiple versions of a record if the source system performs updates; check `write_date` to identify the most recent state.