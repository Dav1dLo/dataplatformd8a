# crm_lead_pls_update_crm_lead_scoring_frequency_field_rel

## Source system
Unknown — insufficient evidence. The table name follows a naming convention often associated with automated schema migrations or temporary relationship mapping tables generated by an ORM or a specific CRM integration tool.

## Functional process 
This table supports the management of lead scoring configurations within a CRM system. It appears to function as a junction table, linking specific lead update events or processes to their associated scoring frequency parameters.

## Description
One row in this table represents a single association between a lead update record and a scoring frequency configuration. It serves as a raw landed link table in the staging layer, facilitating a many-to-many relationship between the two entities.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| crm_lead_pls_update_id | INTEGER | false | Foreign key to the lead update event record | Represents the source entity ID. |
| crm_lead_scoring_frequency_field_id | INTEGER | false | Foreign key to the scoring frequency configuration | Represents the target entity ID. |

## Keys

- **Primary key (inferred):** Not confidently inferable. The table likely uses a composite primary key on `(crm_lead_pls_update_id, crm_lead_scoring_frequency_field_id)`.
- **Foreign keys (inferred):** 
    - `crm_lead_pls_update_id` → `crm_lead_pls_update.id` (Guess: based on naming convention).
    - `crm_lead_scoring_frequency_field_id` → `crm_lead_scoring_frequency_field.id` (Guess: based on naming convention).
- **Natural keys (inferred):** Not confidently inferable.

## Caveats for downstream consumers

- This table is a junction table; ensure joins are handled carefully to avoid fan-out issues.
- No audit timestamps (e.g., `created_at`) are present; incremental loading logic cannot rely on standard watermarking columns.
- The table name suggests it may be a transient or generated artifact; verify if this table is intended for long-term analytical use.