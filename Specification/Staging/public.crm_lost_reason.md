# crm_lost_reason

## Source system
This table originates from an Odoo ERP or CRM system. The naming convention of columns such as `create_uid`, `write_uid`, `create_date`, and `write_date`, combined with the use of `JSONB` for the `name` field and sequence-based primary keys, is characteristic of the Odoo ORM metadata structure.

## Functional process 
This table supports the sales pipeline management process by categorizing the reasons why sales opportunities or leads are marked as "lost." It provides the lookup values used by the CRM module to track churn or conversion failure trends.

## Description
One row in this table represents a single defined reason for losing a sales opportunity. This is a raw landed copy of the configuration entity from the source system, intended to be used as a lookup dimension in downstream reporting.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by the source system. |
| create_uid | INTEGER | true | User ID who created the record | References the system user table. |
| write_uid | INTEGER | true | User ID who last updated the record | References the system user table. |
| name | JSONB | false | The lost reason label | Likely contains multi-language strings; requires extraction. |
| active | BOOLEAN | true | Soft-delete flag | If false, the reason is no longer available for selection. |
| create_date | TIMESTAMP | true | Record creation timestamp | Assumed UTC. |
| write_date | TIMESTAMP | true | Last update timestamp | Assumed UTC. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (guess based on Odoo naming convention).
    - `write_uid` → `res_users.id` (guess based on Odoo naming convention).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- The `name` column is stored as `JSONB`. Downstream queries will need to use the `->>` operator (e.g., `name->>'en_US'`) to extract the relevant string value.
- Timestamps are assumed to be in UTC, consistent with standard Odoo database configurations.
- The `active` column acts as a soft-delete mechanism; ensure queries filter by `active = true` unless historical analysis of inactive reasons is required.
- This table contains no PII, but `create_uid` and `write_uid` link to internal user identities.