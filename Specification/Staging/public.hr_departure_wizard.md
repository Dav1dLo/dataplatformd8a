# hr_departure_wizard

## Source system
This table originates from an Odoo ERP system. The naming convention (`hr_departure_wizard`), the use of `create_uid`/`write_uid` audit columns, and the sequence-based primary key are characteristic of Odoo's internal ORM-generated tables.

## Functional process 
This table supports the Human Resources offboarding process. It captures the data entered into the "Departure Wizard" UI, which triggers the formal termination workflow for an employee, including the reason for departure, the effective date, and descriptive notes.

## Description
One row represents a single instance of an employee departure record initiated through the HR wizard. As a staging table, it serves as a raw, landed copy of the wizard's state, intended for downstream transformation into HR analytics or payroll termination feeds.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| departure_reason_id | INTEGER | false | Foreign key to departure reason | Links to the lookup table for termination types. |
| employee_id | INTEGER | false | Foreign key to employee | The employee being offboarded. |
| create_uid | INTEGER | true | Creator user ID | ID of the user who initiated the wizard. |
| write_uid | INTEGER | true | Last modifier user ID | ID of the user who last updated the record. |
| departure_date | DATE | false | Effective date of departure | The calendar date the employee leaves. |
| departure_description | TEXT | true | Departure notes | Free-text field for additional context. |
| create_date | TIMESTAMP | true | Record creation timestamp | In UTC. |
| write_date | TIMESTAMP | true | Last modification timestamp | In UTC. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `departure_reason_id` → `hr_departure_reason.id` (Guess: standard Odoo HR schema).
    - `employee_id` → `hr_employee.id` (Guess: standard Odoo HR schema).
    - `create_uid` → `res_users.id` (Guess: standard Odoo audit pattern).
    - `write_uid` → `res_users.id` (Guess: standard Odoo audit pattern).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **Sensitive Data:** The `departure_description` may contain PII or sensitive HR comments; ensure appropriate access controls are applied.
- **Timestamps:** `create_date` and `write_date` are assumed to be in UTC, consistent with standard Odoo deployments.
- **Soft Deletes:** This table does not appear to implement a soft-delete flag; assume rows are hard-deleted if removed from the source.
- **Wizard Nature:** As a "wizard" table, this may contain transient data or incomplete records if the user abandons the process before completion.