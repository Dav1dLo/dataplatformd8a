# hr_employee_hr_skill_rel

## Source system
This table likely originates from an HRIS or Human Capital Management (HCM) system such as Odoo or a similar modular ERP, given the naming convention `hr_employee_hr_skill_rel` which is characteristic of a many-to-many join table generated by ORM frameworks to link employees to their respective skill sets.

## Functional process 
This table supports the Skills Management and Talent Development process. It acts as a bridge to associate specific employees with the technical or soft skills they possess, enabling HR teams to perform gap analysis, resource allocation, and project staffing based on employee competencies.

## Description
One row in this table represents a single association between an employee and a specific skill. It is a raw landed copy of a join table, serving as the foundational link for mapping the `hr_employee` entity to the `hr_skill` entity within the staging layer.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| hr_employee_id | INTEGER | false | Foreign key referencing the employee entity | Represents the unique identifier for the employee. |
| hr_skill_id | INTEGER | false | Foreign key referencing the skill entity | Represents the unique identifier for the skill. |

## Keys

- **Primary key (inferred):** The composite of (`hr_employee_id`, `hr_skill_id`).
- **Foreign keys (inferred):** 
    - `hr_employee_id` → `hr_employee.id`: Links to the employee master record.
    - `hr_skill_id` → `hr_skill.id`: Links to the skill definition master record.
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- This table is a junction table; it contains no descriptive attributes (like proficiency level or certification date), only the relationship link.
- There is no surrogate primary key; queries should join or group using the composite key.
- As a staging table, it may contain orphaned records if referential integrity is not strictly enforced in the source system.
- No soft-delete flags are present; assume this table represents the current state of relationships as captured during the last ingestion.