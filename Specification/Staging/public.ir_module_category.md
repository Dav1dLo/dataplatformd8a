# ir_module_category

## Source system
This table originates from an Odoo ERP system, as evidenced by the `ir_module_category` naming convention, the use of `ir_` (Internal Resource) prefixes, and the presence of standard Odoo audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`).

## Functional process 
This table supports the module management and categorization process within the Odoo framework. It defines the hierarchical structure used to organize software modules (apps) in the system interface, allowing for logical grouping and filtering of available functionality.

## Description
One row in this table represents a single category used to classify software modules within the Odoo ecosystem. It serves as a raw landing copy of the system's internal category definitions, maintaining a parent-child relationship to support nested categorization.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by `ir_module_category_id_seq`. |
| create_uid | INTEGER | true | User ID who created the record | References `res_users.id`. |
| create_date | TIMESTAMP | true | Creation timestamp | UTC assumed. |
| write_date | TIMESTAMP | true | Last modification timestamp | UTC assumed. |
| write_uid | INTEGER | true | User ID who last updated the record | References `res_users.id`. |
| parent_id | INTEGER | true | Parent category ID | References `ir_module_category.id` for hierarchy. |
| name | JSONB | false | Category display name | Multi-language support via JSONB. |
| sequence | INTEGER | true | Sort order index | Used to determine display order in UI. |
| description | JSONB | true | Category description | Multi-language support via JSONB. |
| visible | BOOLEAN | true | Visibility flag | Determines if category is shown in the UI. |
| exclusive | BOOLEAN | true | Exclusivity flag | Indicates if modules in this category are mutually exclusive. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `parent_id` → `ir_module_category.id`: Defines the self-referential hierarchy of categories.
    - `create_uid` → `res_users.id`: Links to the user who performed the creation (guess based on Odoo pattern).
    - `write_uid` → `res_users.id`: Links to the user who performed the last update (guess based on Odoo pattern).
- **Natural keys (inferred):** Not confidently inferable. While `name` is descriptive, Odoo typically relies on the surrogate `id` for internal linking.

## Caveats for downstream consumers

- **JSONB Data:** The `name` and `description` columns contain JSONB objects, likely keyed by language code (e.g., `{"en_US": "Accounting", "fr_FR": "Comptabilité"}`). Ensure your queries handle JSON extraction.
- **Timestamps:** Timestamps are stored in the database server's time (typically UTC in Odoo deployments), but verify against the source configuration.
- **Audit Columns:** `create_uid` and `write_uid` are standard Odoo audit fields; they may be null if the record was created via system initialization or migration scripts.
- **Soft Deletes:** This table does not appear to implement soft deletes (no `active` column). Records are likely hard-deleted if removed from the source.