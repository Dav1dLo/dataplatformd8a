# mail_activity_todo_create

## Source system
This table originates from Odoo (OpenERP), an open-source ERP system. The naming convention `mail_activity_todo_create` and the presence of standard Odoo audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`) are characteristic of Odoo's internal activity tracking and notification framework.

## Functional process 
This table supports the task management and activity scheduling process within the CRM or project modules. It tracks "To-Do" items or follow-up activities assigned to users, capturing the summary of the task, the deadline, and associated notes for record-keeping.

## Description
One row in this table represents a single scheduled activity or "to-do" item created within the system. It serves as a raw landing copy of the activity record, capturing the state of the task at the time of ingestion. The grain is one row per activity instance.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| user_id | INTEGER | false | Assigned user ID | Foreign key to the user responsible for the activity. |
| create_uid | INTEGER | true | Creator user ID | ID of the user who initially created the record. |
| write_uid | INTEGER | true | Last modifier user ID | ID of the user who last updated the record. |
| summary | VARCHAR | true | Activity summary | Short description or title of the activity. |
| date_deadline | DATE | false | Due date | The date by which the activity should be completed. |
| note | TEXT | true | Detailed notes | Rich text or plain text notes associated with the activity. |
| create_date | TIMESTAMP | true | Creation timestamp | Timestamp when the record was first created. |
| write_date | TIMESTAMP | true | Last update timestamp | Timestamp when the record was last modified. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `user_id` → `res_users.id` (Standard Odoo pattern for user assignments).
    - `create_uid` → `res_users.id` (Standard Odoo pattern for audit trails).
    - `write_uid` → `res_users.id` (Standard Odoo pattern for audit trails).
- **Natural keys (inferred):** Not confidently inferable.

## Caveats for downstream consumers

- **PII:** The `note` and `summary` columns may contain unstructured PII or sensitive business communications; ensure appropriate masking if exposed to non-authorized users.
- **Timezones:** Timestamps (`create_date`, `write_date`) are typically stored in UTC in Odoo, but verify against the source configuration.
- **Soft Deletes:** This table does not appear to have an `active` boolean flag; assume rows are hard-deleted if they disappear from the source.
- **Data Integrity:** `VARCHAR` and `TEXT` fields may contain HTML tags if the source Odoo instance uses the WYSIWYG editor for notes.