# mail_canned_response

## Source system
This table originates from an Odoo ERP instance, as evidenced by the naming convention (`mail_canned_response`), the use of `create_uid`/`write_uid` audit columns, and the specific sequence-based default value pattern (`nextval('"public".mail_canned_response_id_seq'::regclass)`).

## Functional process 
This table supports the email communication and customer support module, specifically managing "canned responses" or email templates used by agents to standardize replies. It tracks the content of these templates, their usage frequency, and their visibility (shared vs. private) across the organization.

## Description
One row represents a single canned response template available for use in the mail system. It stores the template's trigger keyword, the actual text substitution, and metadata regarding its creation, modification, and last usage. This is a raw landed staging table representing the configuration state of email templates within the application.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| create_uid | INTEGER | true | User ID who created the record | References `res_users.id`. |
| write_uid | INTEGER | true | User ID who last modified the record | References `res_users.id`. |
| source | VARCHAR | false | Trigger keyword or shortcut | Used to invoke the template. |
| description | VARCHAR | true | Human-readable description | Summary of the template's purpose. |
| substitution | TEXT | false | The template body content | The actual text to be inserted. |
| is_shared | BOOLEAN | true | Visibility flag | If true, available to all users. |
| last_used | TIMESTAMP | true | Last usage timestamp | Used for analytics on template popularity. |
| create_date | TIMESTAMP | true | Record creation timestamp | UTC assumed based on Odoo standards. |
| write_date | TIMESTAMP | true | Last modification timestamp | UTC assumed based on Odoo standards. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Guess: standard Odoo audit column).
    - `write_uid` → `res_users.id` (Guess: standard Odoo audit column).
- **Natural keys (inferred):** 
    - `source` (Assuming the shortcut keyword is unique within the system context).

## Caveats for downstream consumers

- **Sensitive Data:** The `substitution` column may contain PII or internal communication templates; ensure appropriate access controls.
- **Timezones:** Timestamps (`last_used`, `create_date`, `write_date`) are typically stored in UTC in Odoo; verify against system configuration if local time conversion is required.
- **Soft Deletes:** This table does not appear to have a `deleted` or `active` flag; assume all records present are active unless an `active` column is added in future schema versions.
- **Data Quality:** The `source` column is a `VARCHAR` without defined length; downstream systems should handle potential variable-length strings gracefully.