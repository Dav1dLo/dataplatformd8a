# mail_notification_mail_resend_message_rel

## Source system
This table originates from an Odoo ERP system. The naming convention `_rel` combined with the two integer columns representing foreign keys is a standard pattern used by Odoo to manage many-to-many relationship tables between two entities.

## Functional process 
This table supports the communication and notification management process. It tracks the association between specific mail notification records and the messages generated by the mail resend functionality, ensuring that resend attempts are correctly linked to their parent notification events.

## Description
One row in this table represents a single link between a mail notification and a mail resend message. It serves as a junction table in the staging layer, providing a raw, normalized view of the many-to-many relationship between these two entities as they exist in the source database.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| mail_resend_message_id | INTEGER | false | Foreign key to the mail resend message entity. | Links to the primary key of the mail_resend_message table. |
| mail_notification_id | INTEGER | false | Foreign key to the mail notification entity. | Links to the primary key of the mail_notification table. |

## Keys

- **Primary key (inferred):** The combination of `(mail_resend_message_id, mail_notification_id)` is the inferred composite primary key.
- **Foreign keys (inferred):** 
    - `mail_resend_message_id` → `mail_resend_message.id`: This column references the specific message record being resent.
    - `mail_notification_id` → `mail_notification.id`: This column references the parent notification record.
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- This is a junction table; queries should typically involve a `JOIN` to the parent tables to retrieve meaningful business attributes.
- There are no timestamps or audit columns present in this table; it represents a static snapshot of the relationship.
- The table does not contain PII, but it links entities that may contain sensitive communication content.