# mail_push

## Source system
This table originates from an Odoo ERP system, as evidenced by the naming convention of audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`) and the use of PostgreSQL sequence-based defaults (`nextval('"public".mail_push_id_seq'::regclass)`).

## Functional process 
This table supports the notification and messaging delivery pipeline, specifically tracking push notifications sent to mobile or web devices. It acts as a staging log for the payload and metadata associated with individual push notification events.

## Description
Each row represents a single push notification event or record associated with a specific device. This table serves as a raw landed copy of notification data, capturing the content payload and the audit trail of who created or modified the record.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by the source system. |
| mail_push_device_id | INTEGER | false | Foreign key to the device record | Links the notification to a specific target device. |
| create_uid | INTEGER | true | User ID who created the record | References the internal user ID of the creator. |
| write_uid | INTEGER | true | User ID who last updated the record | References the internal user ID of the last modifier. |
| payload | TEXT | true | Notification content | Likely contains JSON or raw text sent to the device. |
| create_date | TIMESTAMP | true | Creation timestamp | Recorded by the source system. |
| write_date | TIMESTAMP | true | Last update timestamp | Recorded by the source system. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `mail_push_device_id` → `mail_push_device.id` (Guess: standard Odoo naming convention for device associations).
    - `create_uid` → `res_users.id` (Guess: standard Odoo audit column referencing the user table).
    - `write_uid` → `res_users.id` (Guess: standard Odoo audit column referencing the user table).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **PII/Sensitive Data:** The `payload` column may contain sensitive user information or message content; ensure appropriate masking if exposed to non-authorized users.
- **Timezone:** Timestamps (`create_date`, `write_date`) are typically stored in UTC in Odoo-based systems, but verify against the source configuration.
- **Soft Deletes:** This table does not appear to have a `deleted_at` or `active` flag; assume all records are active unless otherwise specified by the source system logic.
- **Data Quality:** The `payload` column is `TEXT` and may contain unstructured or semi-structured data; downstream parsing will be required.