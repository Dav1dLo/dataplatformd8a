# mrp_bom_byproduct

## Source system
This table originates from Odoo ERP, as evidenced by the naming convention `mrp_bom_byproduct` (Manufacturing Resource Planning Bill of Materials Byproduct), the use of `create_uid`/`write_uid` audit columns, and the specific sequence-based primary key pattern common to Odoo's PostgreSQL schema.

## Functional process 
This table supports the manufacturing bill of materials (BOM) management process, specifically tracking secondary outputs (byproducts) generated during the production of a primary product. It links specific products to a BOM and defines the quantity and cost allocation for items produced alongside the main output.

## Description
One row represents a single byproduct definition associated with a specific Bill of Materials in the manufacturing module. It captures the quantity, unit of measure, and cost share percentage for a byproduct produced during a specific manufacturing operation. This is a raw landed copy of the Odoo staging table, intended for downstream transformation into manufacturing analytics models.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| product_id | INTEGER | false | Product identifier | Foreign key to the product master. |
| company_id | INTEGER | true | Company identifier | Multi-company context for the record. |
| product_uom_id | INTEGER | false | Unit of measure identifier | Reference to the UOM master. |
| bom_id | INTEGER | true | Bill of Materials identifier | Links the byproduct to a parent BOM. |
| operation_id | INTEGER | true | Manufacturing operation identifier | Links the byproduct to a specific step in the routing. |
| sequence | INTEGER | true | Display sequence | Used for ordering byproducts in the UI. |
| create_uid | INTEGER | true | Creator user ID | ID of the user who created the record. |
| write_uid | INTEGER | true | Last updater user ID | ID of the user who last modified the record. |
| product_qty | NUMERIC | false | Byproduct quantity | The amount produced; unit depends on `product_uom_id`. |
| cost_share | NUMERIC | true | Cost allocation percentage | Percentage of the total production cost assigned to this byproduct. |
| create_date | TIMESTAMP | true | Creation timestamp | Recorded in server local time. |
| write_date | TIMESTAMP | true | Last update timestamp | Recorded in server local time. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):**
    - `product_id` → `product_product.id` (Standard Odoo link to product master)
    - `company_id` → `res_company.id` (Standard Odoo multi-company link)
    - `product_uom_id` → `uom_uom.id` (Standard Odoo link to units of measure)
    - `bom_id` → `mrp_bom.id` (Links to the parent Bill of Materials)
    - `operation_id` → `mrp_routing_workcenter.id` (Links to specific manufacturing operation)
- **Natural keys (inferred):** Not confidently inferable; Odoo typically relies on the surrogate `id` for internal references.

## Caveats for downstream consumers

- **Timestamps:** Timestamps are stored in the Odoo server's local time; verify the server timezone configuration before performing time-series analysis.
- **Soft Deletes:** This table does not appear to implement a soft-delete flag (e.g., `active` column); assume records are hard-deleted if removed from the source.
- **Cost Share:** The `cost_share` column is a percentage; ensure downstream logic handles potential nulls as 0% or default allocation.
- **Data Integrity:** As a staging table, this may contain orphaned records if the parent `mrp_bom` or `product` records were deleted in the source system.