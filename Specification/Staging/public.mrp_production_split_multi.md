# mrp_production_split_multi

## Source system
The table originates from an Odoo ERP system, as evidenced by the naming convention (`mrp_production_split_multi`), the use of `create_uid`/`write_uid` audit columns, and the specific sequence-based default value pattern (`nextval('"public".mrp_production_split_multi_id_seq'::regclass)`).

## Functional process 
This table supports the Manufacturing Resource Planning (MRP) process, specifically managing the splitting of production orders into multiple sub-batches or work orders. It tracks the administrative lifecycle of these split operations within the production module.

## Description
One row in this table represents a single administrative record of a production split event within the manufacturing module. It serves as a raw landed copy of the Odoo system's internal tracking table, capturing the audit trail for when a production order was divided.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| create_uid | INTEGER | true | User ID who created the record | References `res_users.id`. |
| write_uid | INTEGER | true | User ID who last modified the record | References `res_users.id`. |
| create_date | TIMESTAMP | true | Creation timestamp | Inferred UTC based on Odoo standards. |
| write_date | TIMESTAMP | true | Last modification timestamp | Inferred UTC based on Odoo standards. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Standard Odoo audit pattern).
    - `write_uid` → `res_users.id` (Standard Odoo audit pattern).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- Timestamps (`create_date`, `write_date`) are assumed to be in UTC, consistent with standard Odoo database configurations.
- This table contains audit metadata; it does not appear to contain the actual business logic or quantities of the production split, which likely reside in related tables.
- No PII is present in this table, as it only contains system-generated IDs and timestamps.
- The table does not implement soft-delete flags; standard Odoo behavior is to hard-delete records unless an `active` column is present.