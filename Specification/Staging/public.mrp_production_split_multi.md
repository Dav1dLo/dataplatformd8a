# mrp_production_split_multi

## Source system
This table originates from Odoo ERP, as evidenced by the naming convention (`mrp_production_split_multi`), the use of `create_uid`/`write_uid` audit columns, and the specific sequence-based default value pattern (`nextval('"public".mrp_production_split_multi_id_seq'::regclass)`).

## Functional process 
This table supports the Manufacturing Resource Planning (MRP) process, specifically handling the splitting of production orders into multiple sub-batches or partial production runs. It tracks the audit trail of these split operations within the production management module.

## Description
One row in this table represents a single record of a production split operation within the manufacturing module. It serves as a raw landed copy of the Odoo staging data, capturing the audit metadata for split events. The grain is one row per production split record.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| create_uid | INTEGER | true | User ID who created the record | References `res_users.id`. |
| write_uid | INTEGER | true | User ID who last modified the record | References `res_users.id`. |
| create_date | TIMESTAMP | true | Record creation timestamp | Assumed UTC. |
| write_date | TIMESTAMP | true | Last modification timestamp | Assumed UTC. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id`: Standard Odoo audit pattern for record creation.
    - `write_uid` → `res_users.id`: Standard Odoo audit pattern for record modification.
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- Timestamps (`create_date`, `write_date`) are assumed to be in UTC, consistent with standard Odoo database configurations.
- This table contains audit metadata; it does not appear to contain the business logic or quantities associated with the split itself, suggesting it may be a join table or a header for a split process.
- No soft-delete flag is present; assume standard Odoo behavior where records are either hard-deleted or maintained indefinitely.
- The `id` column is managed by a PostgreSQL sequence; do not attempt to insert values manually without accounting for the sequence state.