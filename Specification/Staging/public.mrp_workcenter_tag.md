# mrp_workcenter_tag

## Source system
This table originates from an Odoo ERP system, as evidenced by the naming convention (`mrp_workcenter_tag`), the use of `create_uid`/`write_uid` audit columns, and the standard sequence-based primary key pattern (`nextval` on a `_seq` object).

## Functional process 
This table supports the Manufacturing Resource Planning (MRP) module, specifically the categorization or tagging of work centers. It allows production managers to group work centers by attributes (e.g., "High Precision", "Maintenance Required", "CNC") to facilitate scheduling and reporting within the manufacturing pipeline.

## Description
One row represents a single tag definition that can be associated with manufacturing work centers. This is a raw landed copy of the Odoo configuration table, serving as a reference entity for downstream manufacturing analytics and work center categorization.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by the source system. |
| color | INTEGER | true | UI color index | Represents the color code used in the Odoo frontend. |
| create_uid | INTEGER | true | Creator user ID | Foreign key to the system user who created the tag. |
| write_uid | INTEGER | true | Last modifier user ID | Foreign key to the system user who last updated the tag. |
| name | VARCHAR | false | Tag label | The human-readable name of the work center tag. |
| create_date | TIMESTAMP | true | Creation timestamp | Recorded in UTC by the source system. |
| write_date | TIMESTAMP | true | Last update timestamp | Recorded in UTC by the source system. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Guess: standard Odoo audit column linking to the user table).
    - `write_uid` → `res_users.id` (Guess: standard Odoo audit column linking to the user table).
- **Natural keys (inferred):** 
    - `name` (Assuming tag names are unique within the Odoo instance).

## Caveats for downstream consumers

- **Timestamps:** Timestamps are assumed to be in UTC, consistent with standard Odoo database configurations.
- **Audit Columns:** `create_uid` and `write_uid` refer to internal system user IDs; these will not resolve to meaningful names without joining to the `res_users` table.
- **Data Integrity:** As a staging table, this contains raw data; verify if the source system enforces uniqueness on the `name` column before using it as a join key in downstream models.
- **Soft Deletes:** This table does not appear to have a `deleted` or `active` flag; assume all rows are currently active unless otherwise specified by the source system logic.