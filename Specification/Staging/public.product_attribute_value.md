# product_attribute_value

## Source system
The table likely originates from an Odoo ERP system. The naming convention (e.g., `create_uid`, `write_uid`, `create_date`, `write_date`), the use of `JSONB` for translatable fields like `name`, and the specific sequence pattern `nextval('"public".product_attribute_value_id_seq'::regclass)` are characteristic of Odoo's PostgreSQL schema architecture.

## Functional process 
This table supports the Product Catalog management process, specifically handling the definition of attribute values (e.g., "Red", "Large", "Cotton") associated with product variants. It links specific attribute options to products and manages their display properties, pricing adjustments, and custom configuration status.

## Description
One row represents a single value for a specific product attribute (e.g., the value "Blue" for the attribute "Color"). This is a raw landing table in the staging layer, containing the direct, un-transformed state of attribute value definitions as they exist in the source ERP.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by source. |
| sequence | INTEGER | true | Display order | Used for sorting values in UI. |
| attribute_id | INTEGER | false | Foreign key to product_attribute | Links value to its parent attribute. |
| color | INTEGER | true | Color index | Integer representation of color. |
| create_uid | INTEGER | true | Creator user ID | Reference to the user who created the record. |
| write_uid | INTEGER | true | Last modifier user ID | Reference to the user who last updated the record. |
| html_color | VARCHAR | true | CSS color code | Hex or named color string (e.g., #FF0000). |
| name | JSONB | false | Attribute value name | Multi-language label stored as JSON. |
| is_custom | BOOLEAN | true | Custom flag | Indicates if the value is user-defined. |
| active | BOOLEAN | true | Soft-delete flag | False indicates the value is archived. |
| create_date | TIMESTAMP | true | Creation timestamp | UTC timestamp of record creation. |
| write_date | TIMESTAMP | true | Last update timestamp | UTC timestamp of last modification. |
| default_extra_price | DOUBLE PRECISION | true | Price adjustment | Additional cost associated with this value. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `attribute_id` → `product_attribute.id`: Links the value to the definition of the attribute it belongs to.
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **PII/Sensitivity:** Contains user IDs (`create_uid`, `write_uid`) which may link to internal employee records.
- **Timezones:** Timestamps are assumed to be in UTC, consistent with standard Odoo/PostgreSQL configurations.
- **Soft Deletes:** The `active` column should be filtered (`WHERE active = TRUE`) for most business reporting to exclude archived attribute values.
- **Data Structure:** The `name` column is `JSONB`; ensure your query logic handles JSON extraction (e.g., `name->>'en_US'`) if specific language labels are required.
- **Precision:** `default_extra_price` is `DOUBLE PRECISION`; be aware of potential floating-point rounding issues when performing financial aggregations.