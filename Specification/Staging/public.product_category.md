# product_category

## Source system
This table originates from an Odoo ERP system. The naming conventions, specifically the presence of `create_uid`, `write_uid`, `parent_path`, and the extensive use of `JSONB` fields for property definitions, are characteristic of the Odoo ORM's internal structure for hierarchical data models.

## Functional process 
This table supports the product catalog management and inventory accounting configuration process. It defines the hierarchical structure of product categories, which dictates how products are grouped for reporting, and stores the default accounting and valuation rules (e.g., income/expense accounts, cost methods) that are inherited by products within those categories.

## Description
One row in this table represents a single node in the product category hierarchy, which may be a top-level category or a sub-category. This is a raw landed copy of the Odoo `product.category` model, capturing both the structural tree data and the associated configuration properties for inventory and accounting.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| parent_id | INTEGER | true | Self-referencing foreign key | Points to the parent category ID. |
| create_uid | INTEGER | true | Creator user ID | ID of the user who created the record. |
| write_uid | INTEGER | true | Last modifier user ID | ID of the user who last updated the record. |
| name | VARCHAR | false | Category name | The display name of the category. |
| complete_name | VARCHAR | true | Full hierarchical path name | Concatenated path of parent names. |
| parent_path | VARCHAR | true | Materialized path | Used for efficient tree traversal. |
| product_properties_definition | JSONB | true | Custom property definitions | Schema for dynamic product attributes. |
| create_date | TIMESTAMP | true | Creation timestamp | Recorded in server local time. |
| write_date | TIMESTAMP | true | Last update timestamp | Recorded in server local time. |
| property_account_income_categ_id | JSONB | true | Income account mapping | Default income account for products in this category. |
| property_account_expense_categ_id | JSONB | true | Expense account mapping | Default expense account for products in this category. |
| removal_strategy_id | INTEGER | true | Inventory removal strategy | Foreign key to stock removal strategy. |
| packaging_reserve_method | VARCHAR | true | Packaging reserve method | Strategy for reserving stock. |
| property_valuation | JSONB | true | Valuation method | Inventory valuation configuration. |
| property_cost_method | JSONB | true | Costing method | Method for calculating product costs. |
| property_stock_journal | JSONB | true | Stock journal mapping | Default journal for stock moves. |
| property_stock_account_input_categ_id | JSONB | true | Stock input account | Account for incoming stock valuation. |
| property_stock_account_output_categ_id | JSONB | true | Stock output account | Account for outgoing stock valuation. |
| property_stock_valuation_account_id | JSONB | true | Stock valuation account | Account for inventory asset value. |
| property_stock_account_production_cost_id | JSONB | true | Production cost account | Account for production cost variance. |
| property_account_creditor_price_difference_categ | JSONB | true | Price difference account | Account for purchase price variances. |
| property_account_downpayment_categ_id | JSONB | true | Downpayment account | Account for customer downpayments. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `parent_id` → `public.product_category.id`: Establishes the parent-child relationship for the category tree.
    - `removal_strategy_id` → `public.product_removal_strategy.id` (guess): Likely links to a lookup table defining stock removal logic.
- **Natural keys (inferred):** 
    - `complete_name`: In Odoo, the full path is typically unique within the category tree.

## Caveats for downstream consumers

- **Sensitive Data:** Contains accounting configuration and user IDs; ensure access is restricted to authorized financial and supply chain analysts.
- **Timestamps:** Timestamps are stored in the server's local time; verify the Odoo server timezone configuration (usually UTC) before performing time-series analysis.
- **JSONB Fields:** Many columns are `JSONB` types. These often contain Odoo "property" references (e.g., `{"value": 123}`). You will need to use the `->>` operator to extract the underlying ID or value for joins.
- **Soft Deletes:** This table does not implement a soft-delete flag; records are typically physically deleted in the source system.
- **Hierarchy:** Use the `parent_path` column for efficient recursive queries rather than self-joining on `parent_id` where possible.