# product_tag

## Source system
This table originates from an Odoo ERP system. The presence of `create_uid`, `write_uid`, `create_date`, and `write_date` columns, alongside the use of `JSONB` for the `name` field (common for multi-language support in Odoo), is characteristic of the Odoo ORM metadata pattern.

## Functional process 
This table supports the product categorization and tagging process within the inventory or e-commerce module. It allows users to define custom labels or tags that can be associated with products to facilitate filtering, reporting, and storefront organization.

## Description
One row represents a single product tag definition available for assignment to products. This is a raw landed copy of the Odoo `product.tag` model, serving as the staging entity for downstream product dimension modeling.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| sequence | INTEGER | true | Display order index | Used to sort tags in the UI. |
| create_uid | INTEGER | true | Creator user ID | References the user who created the tag. |
| write_uid | INTEGER | true | Last modifier user ID | References the user who last updated the tag. |
| color | VARCHAR | true | UI color identifier | Often an integer stored as a string or a CSS class name. |
| name | JSONB | false | Tag display name | Multilingual dictionary; check keys for language codes. |
| create_date | TIMESTAMP | true | Record creation timestamp | UTC timestamp. |
| write_date | TIMESTAMP | true | Last update timestamp | UTC timestamp. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Guess: standard Odoo audit column).
    - `write_uid` → `res_users.id` (Guess: standard Odoo audit column).
- **Natural keys (inferred):** 
    - The `name` JSONB field is likely the business key, though uniqueness is often enforced at the application level rather than the database level.

## Caveats for downstream consumers

- **JSONB Handling:** The `name` column contains localized strings. Queries should use the `->>` operator (e.g., `name->>'en_US'`) to extract specific language values.
- **Timestamps:** Timestamps are assumed to be in UTC, consistent with standard Odoo database configurations.
- **Soft Deletes:** This table does not appear to have a `deleted` or `active` flag; assume all rows are active unless an `active` column is added in future schema versions.
- **Audit Columns:** `create_uid` and `write_uid` refer to internal Odoo user IDs; these will not resolve to meaningful names without joining to the `res_users` table.