# product_template_attribute_exclusion

## Source system
This table originates from Odoo (ERP), as evidenced by the naming convention `product_template_attribute_exclusion` and the presence of standard Odoo audit columns (`create_uid`, `write_uid`, `create_date`, `write_