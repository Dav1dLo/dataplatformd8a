# project_share_wizard_res_partner_rel

## Source system
This table originates from an Odoo ERP system. The naming convention `res_partner` is the standard internal identifier for the "Partner" (customer/vendor/contact) entity in Odoo, and the `_rel` suffix indicates a join table generated by the Odoo ORM to manage a many-to-many relationship between a wizard (a temporary UI-driven process) and partners.

## Functional process 
This table supports the "Project Sharing" business process, specifically managing the association between a project sharing wizard instance and the specific partners (contacts) who have been granted access or are being invited to collaborate on a project. It facilitates the mapping of multiple partners to a single sharing configuration session.

## Description
One row in this table represents a single link between a specific project share wizard instance and a partner record. It serves as a raw landing copy of the many-to-many relationship table from the source database, capturing the association at the grain of one row per wizard-partner pair.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| project_share_wizard_id | INTEGER | false | Foreign key to the project share wizard instance | Links to the wizard configuration record. |
| res_partner_id | INTEGER | false | Foreign key to the partner record | Links to the contact/partner being shared with. |

## Keys

- **Primary key (inferred):** Not confidently inferable; likely a composite primary key on (`project_share_wizard_id`, `res_partner_id`).
- **Foreign keys (inferred):** 
    - `project_share_wizard_id` → `project_share_wizard.id`: This column represents the parent wizard session.
    - `res_partner_id` → `res_partner.id`: This column represents the associated contact entity.
- **Natural keys (inferred):** Not confidently inferable.

## Caveats for downstream consumers

- This is a pure join table; it contains no descriptive attributes, only identifiers.
- There are no timestamps or audit columns present in this table; it is strictly a structural mapping.
- As a staging table, it should be joined against the corresponding `project_share_wizard` and `res_partner` tables to derive meaningful business context.
- Ensure referential integrity checks are performed when joining, as staging tables may contain orphaned records if the source system's cleanup processes are inconsistent.