# project_tags_project_task_rel

## Source system
The table likely originates from a project management or task-tracking system (e.g., Jira, Asana, or a custom internal tool). The naming convention `_rel` combined with the two foreign key columns strongly suggests this is a junction table generated by an ORM or a relational database schema to manage a many-to-many relationship between project tasks and tags.

## Functional process 
This table supports the categorization and metadata tagging process within a project management workflow. It enables the association of multiple descriptive tags with individual project tasks, allowing for filtering, reporting, and organizational grouping of tasks across the platform.

## Description
One row in this table represents a single association between a specific project task and a specific tag. It serves as a raw, normalized link table in the staging layer, capturing the many-to-many relationship required to map tags to tasks without duplicating data in the primary task or tag entities.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| project_task_id | INTEGER | false | Surrogate key referencing the project task | Links to the primary task entity. |
| project_tags_id | INTEGER | false | Surrogate key referencing the tag definition | Links to the tag metadata entity. |

## Keys

- **Primary key (inferred):** Composite key of (`project_task_id`, `project_tags_id`).
- **Foreign keys (inferred):** 
    - `project_task_id` → `public.project_tasks.id` (inferred based on standard naming conventions for task entities).
    - `project_tags_id` → `public.project_tags.id` (inferred based on standard naming conventions for tag entities).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- This table is a pure junction table; it contains no descriptive attributes, only identifiers.
- There are no timestamps or soft-delete flags present; assume this table reflects the current state of associations as captured during the last ingestion.
- Ensure that joins to the parent tables (`project_tasks` and `project_tags`) handle potential orphans if referential integrity is not strictly enforced in the source system.