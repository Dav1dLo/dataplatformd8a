# quotation_document_sale_order_rel

## Source system
The table likely originates from an ERP or CRM system (such as Odoo or a custom-built sales management platform) where sales orders and quotation documents are managed as distinct entities. The naming convention `_rel` strongly suggests this is a join table generated by an ORM or a relational database schema to manage a many-to-many or one-to-many relationship between sales orders and quotation documents.

## Functional process 
This table supports the Sales-to-Order lifecycle, specifically tracking the lineage between formal quotation documents issued to customers and the resulting sales orders. It ensures that the business can audit which specific quotation document led to the creation of a confirmed sales order.

## Description
One row in this table represents a single association between a sales order and a quotation document. It serves as a raw landing copy of a link table, capturing the relationship grain at the intersection of the two entities.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| sale_order_id | INTEGER | false | Unique identifier for the sales order. | Foreign key to the sales order master table. |
| quotation_document_id | INTEGER | false | Unique identifier for the quotation document. | Foreign key to the quotation document master table. |

## Keys

- **Primary key (inferred):** The composite of (`sale_order_id`, `quotation_document_id`).
- **Foreign keys (inferred):** 
    - `sale_order_id` → `sale_order.id`: Links to the primary sales order record.
    - `quotation_document_id` → `quotation_document.id`: Links to the primary quotation document record.
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- This is a link table; it contains no descriptive attributes, only identifiers.
- There is no audit timestamp (e.g., `created_at`) provided in this schema, making it difficult to determine the chronological order of associations without joining to the parent tables.
- Ensure referential integrity is checked against the parent tables, as staging tables may contain orphaned records if the ingestion process is not synchronized.