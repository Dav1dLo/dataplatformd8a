# res_country_state

## Source system
This table originates from an Odoo ERP system, as evidenced by the `res_country_state` naming convention, the use of `res_` prefixing for resource tables, and the standard Odoo audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`).

## Functional process 
This table supports the geographical master data management process, specifically maintaining the registry of states, provinces, or regions associated with countries. It is used to populate address forms, validate shipping/billing locations, and support regional tax calculations within the ERP.

## Description
One row in this table represents a single administrative subdivision (state or province) within a specific country. It serves as a raw landed copy of the Odoo `res.country.state` model, providing the foundational lookup data for regional address components across the platform.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| country_id | INTEGER | false | Foreign key to country | Links to the parent country record. |
| create_uid | INTEGER | true | Creator user ID | ID of the user who created the record. |
| write_uid | INTEGER | true | Last modifier user ID | ID of the user who last updated the record. |
| name | VARCHAR | false | State/Province name | Full display name of the region. |
| code | VARCHAR | false | State/Province code | Short code (e.g., ISO-3166-2 or postal abbreviation). |
| create_date | TIMESTAMP | true | Creation timestamp | Recorded by the ingestion job; timezone unknown. |
| write_date | TIMESTAMP | true | Last update timestamp | Recorded by the ingestion job; timezone unknown. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `country_id` → `res_country.id`: This column links the state to its parent country record in the Odoo schema.
- **Natural keys (inferred):** 
    - `(country_id, code)`: The combination of country and state code is typically unique within the Odoo system.

## Caveats for downstream consumers

- **Timestamps:** `create_date` and `write_date` are provided as-is from the source; verify if these are stored in UTC or server-local time before performing time-series analysis.
- **Data Integrity:** This is a raw staging table; it may contain duplicate entries or legacy codes if the source system has undergone data migrations.
- **Soft Deletes:** This table does not appear to implement a soft-delete flag; assume all rows are active unless otherwise filtered by business logic.
- **PII:** No sensitive PII is contained in this table.