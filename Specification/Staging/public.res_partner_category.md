# res_partner_category

## Source system
This table originates from Odoo (formerly OpenERP), an open-source ERP system. The naming convention `res_partner_category` is a standard Odoo model name for partner tags or categories, and the presence of `create_uid`, `write_uid`, and `JSONB` name fields is characteristic of Odoo's PostgreSQL-based architecture.

## Functional process 
This table supports the Customer Relationship Management (CRM) and Partner Management processes by providing a hierarchical tagging system for contacts and companies. It allows users to categorize partners (e.g., "VIP", "Supplier", "Lead") to facilitate filtering, segmentation, and reporting across the sales and procurement pipelines.

## Description
One row in this table represents a single category or tag definition used to classify partners within the system. It maintains a recursive structure via `parent_id` to support nested category trees. As a staging table, it serves as a raw, direct reflection of the Odoo `res_partner_category` table, capturing the current state of category definitions.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| color | INTEGER | true | UI color index | Used for visual representation in the Odoo frontend. |
| parent_id | INTEGER | true | Parent category ID | References `id` in this same table for hierarchy. |
| create_uid | INTEGER | true | Creator user ID | References `res_users.id`. |
| write_uid | INTEGER | true | Last modifier user ID | References `res_users.id`. |
| parent_path | VARCHAR | true | Materialized path | Used for efficient recursive tree queries. |
| name | JSONB | false | Category name | Multilingual support; stores name translations. |
| active | BOOLEAN | true | Soft-delete flag | If false, the category is hidden from the UI. |
| create_date | TIMESTAMP | true | Creation timestamp | UTC timestamp of record creation. |
| write_date | TIMESTAMP | true | Last update timestamp | UTC timestamp of last modification. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `parent_id` → `res_partner_category.id`: Defines the parent-child relationship in the category hierarchy.
    - `create_uid` → `res_users.id`: Tracks the user who created the category (guess based on Odoo standard).
    - `write_uid` → `res_users.id`: Tracks the user who last modified the category (guess based on Odoo standard).
- **Natural keys (inferred):** 
    - `name`: While stored as JSONB, the business logic typically enforces unique names within the same parent level.

## Caveats for downstream consumers

- **Sensitive Data:** No PII is present, but `create_uid` and `write_uid` link to internal user records.
- **Timestamps:** Odoo stores timestamps in UTC; ensure downstream transformations account for this.
- **Soft Deletes:** The `active` column is a standard Odoo soft-delete flag. Queries should generally filter by `WHERE active = TRUE` unless historical analysis of deleted categories is required.
- **JSONB Handling:** The `name` column is `JSONB`. Downstream SQL must use the `->>` operator (e.g., `name->>'en_US'`) to extract specific language values.
- **Hierarchy:** The `parent_path` column is a materialized path (e.g., "1/5/12") used for performance; it should be treated as a denormalized helper column.