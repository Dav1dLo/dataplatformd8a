# res_partner_title

## Source system
This table originates from Odoo (formerly OpenERP), as evidenced by the `res_partner_title` naming convention, the use of `create_uid`/`write_uid` audit columns, and the `JSONB` data types for multi-language fields (`name`, `shortcut`).

## Functional process 
This table supports the Customer Relationship Management (CRM) and contact management modules by defining honorific titles or salutations (e.g., "Mr.", "Ms.", "Dr.") used for business partners. It acts as a lookup table to standardize how individuals are addressed in communications and documentation.

## Description
One row in this table represents a single honorific title or salutation available for assignment to a business partner. This is a raw landed copy of the Odoo configuration table, serving as a reference dimension for downstream contact-related entities.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| create_uid | INTEGER | true | Creator user ID | Reference to the user who created this record. |
| write_uid | INTEGER | true | Last modifier user ID | Reference to the user who last updated this record. |
| name | JSONB | false | Title display name | Multi-language string; usually contains the full title. |
| shortcut | JSONB | true | Abbreviated title | Multi-language string; short version of the title. |
| create_date | TIMESTAMP | true | Creation timestamp | Recorded in server time (usually UTC). |
| write_date | TIMESTAMP | true | Last update timestamp | Recorded in server time (usually UTC). |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Guess: standard Odoo audit column linking to the users table).
    - `write_uid` → `res_users.id` (Guess: standard Odoo audit column linking to the users table).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **JSONB Handling:** The `name` and `shortcut` columns contain JSONB data. Query writers must use PostgreSQL JSON operators (e.g., `->>`) to extract specific language values (e.g., `name->>'en_US'`).
- **Timezone:** Timestamps are stored as `TIMESTAMP` without time zone; assume UTC unless the Odoo instance configuration specifies otherwise.
- **Audit Columns:** `create_uid` and `write_uid` are internal Odoo references and may not resolve if the `res_users` table is not present in the same schema.
- **Soft Deletes:** This table does not appear to implement a soft-delete flag; records are assumed to be active unless removed from the source.