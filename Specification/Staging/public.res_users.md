# res_users

## Source system
This table originates from Odoo ERP. The naming convention `res_users` (resource users) and the presence of specific columns like `odoobot_state`, `partner_id`, and `create_uid` are characteristic of the Odoo framework's core user management module.

## Functional process 
This table supports the Identity and Access Management (IAM) process within the ERP. It manages user authentication, system access rights, and user-specific configurations for sales targets and notification preferences across the organization.

## Description
One row in this table represents a single user account within the Odoo system, containing authentication credentials, system preferences, and sales-related performance targets. This is a raw landed copy of the Odoo `res.users` model, serving as the primary source for user-related dimensions in the staging layer.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| company_id | INTEGER | false | Associated company ID | Foreign key to `res_company`. |
| partner_id | INTEGER | false | Associated partner ID | Foreign key to `res_partner`. |
| active | BOOLEAN | true | Soft-delete flag | Defaults to true. |
| create_date | TIMESTAMP | true | Creation timestamp | Inferred UTC. |
| login | VARCHAR | false | User login identifier | Typically an email address. |
| password | VARCHAR | true | Hashed password | Sensitive: contains credential hash. |
| action_id | INTEGER | true | Default action ID | Links to initial view/action. |
| create_uid | INTEGER | true | Creator user ID | Audit trail: user who created this record. |
| write_uid | INTEGER | true | Last modifier user ID | Audit trail: user who last updated this record. |
| signature | TEXT | true | Email signature | User-defined HTML/text signature. |
| share | BOOLEAN | true | Portal user flag | Indicates if user is an external/portal user. |
| write_date | TIMESTAMP | true | Last update timestamp | Inferred UTC. |
| totp_secret | VARCHAR | true | TOTP secret key | Sensitive: used for 2FA. |
| tour_enabled | BOOLEAN | true | Onboarding tour status | Tracks if user has completed tours. |
| notification_type | VARCHAR | false | Notification preference | e.g., 'email', 'inbox'. |
| odoobot_state | VARCHAR | true | OdooBot interaction state | Tracks state of the internal chatbot. |
| odoobot_failed | BOOLEAN | true | OdooBot failure flag | Indicates if bot interaction failed. |
| sale_team_id | INTEGER | true | Default sales team ID | Foreign key to `crm_team`. |
| target_sales_won | INTEGER | true | Sales won target | Numeric target for won opportunities. |
| target_sales_done | INTEGER | true | Sales done target | Numeric target for completed tasks. |
| website_id | INTEGER | true | Associated website ID | Foreign key to `website`. |
| target_sales_invoiced | INTEGER | true | Invoiced sales target | Numeric target for invoiced amounts. |
| property_warehouse_id | JSONB | true | Warehouse preferences | Stores complex warehouse configuration. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `company_id` → `res_company.id` (Standard Odoo multi-company architecture).
    - `partner_id` → `res_partner.id` (Odoo users are linked to a partner record for contact details).
    - `sale_team_id` → `crm_team.id` (Links user to their primary sales team).
- **Natural keys (inferred):** 
    - `login` (Unique identifier for authentication).

## Caveats for downstream consumers

- **Sensitive Data:** The `password` and `totp_secret` columns contain security-sensitive information and should be masked or excluded from non-administrative reporting.
- **Timestamps:** `create_date` and `write_date` are assumed to be in UTC, consistent with standard Odoo database configurations.
- **Soft Deletes:** The `active` column acts as a soft-delete flag; ensure queries filter by `active = true` unless historical/inactive users are explicitly required.
- **JSONB:** The `property_warehouse_id` column contains JSONB data; downstream consumers will need to use PostgreSQL JSON operators (e.g., `->>`) to extract specific values.