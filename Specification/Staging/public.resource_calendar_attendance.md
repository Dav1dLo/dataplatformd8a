# resource_calendar_attendance

## Source system
This table originates from Odoo ERP. The naming convention (`resource_calendar_attendance`), the presence of audit fields like `create_uid` and `write_uid`, and the specific structure of calendar-based scheduling are characteristic of Odoo's resource management module.

## Functional process 
This table supports the resource scheduling and capacity planning process. It defines the specific working hours and attendance intervals for resources (such as employees or equipment) linked to a specific calendar, which is used to calculate availability, working time, and payroll-related attendance tracking.

## Description
One row represents a single recurring or specific attendance interval (e.g., "Monday 09:00 to 17:00") defined within a resource calendar. This is a raw landing table in the staging layer, capturing the configuration of working time rules as defined in the source ERP.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by source. |
| calendar_id | INTEGER | false | Foreign key to the parent calendar | Links to the resource calendar definition. |
| resource_id | INTEGER | true | Foreign key to the specific resource | Optional; if null, applies to all resources in the calendar. |
| sequence | INTEGER | true | Sort order | Used to determine priority of attendance rules. |
| create_uid | INTEGER | true | Creator user ID | ID of the user who created this record. |
| write_uid | INTEGER | true | Last modifier user ID | ID of the user who last updated this record. |
| name | VARCHAR | false | Attendance rule label | Descriptive name for the attendance slot. |
| dayofweek | VARCHAR | false | Day of the week | Typically '0' (Monday) through '6' (Sunday). |
| day_period | VARCHAR | false | Time of day period | e.g., 'morning', 'afternoon'. |
| week_type | VARCHAR | true | Week cycle type | Used for bi-weekly or alternating schedules. |
| display_type | VARCHAR | true | UI display category | Used to distinguish line types in the Odoo UI. |
| date_from | DATE | true | Start date of validity | The date from which this rule is active. |
| date_to | DATE | true | End date of validity | The date until which this rule is active. |
| create_date | TIMESTAMP | true | Creation timestamp | UTC timestamp of record creation. |
| write_date | TIMESTAMP | true | Last update timestamp | UTC timestamp of last modification. |
| hour_from | DOUBLE PRECISION | false | Start time | Represented as a decimal (e.g., 9.5 for 09:30). |
| hour_to | DOUBLE PRECISION | false | End time | Represented as a decimal (e.g., 17.0 for 17:00). |
| duration_days | DOUBLE PRECISION | true | Duration in days | Calculated duration of the attendance slot. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `calendar_id` → `resource_calendar.id` (Evidence: standard Odoo naming convention for parent-child relationships).
    - `resource_id` → `resource_resource.id` (Evidence: standard Odoo naming convention for resource links).
- **Natural keys (inferred):** Not confidently inferable.

## Caveats for downstream consumers

- **Time Representation:** `hour_from` and `hour_to` are stored as `DOUBLE PRECISION` decimals representing hours from midnight (e.g., 14.5 = 14:30). Ensure conversion logic is applied for time-based calculations.
- **Timezone:** Timestamps (`create_date`, `write_date`) are assumed to be in UTC, consistent with standard Odoo database configurations.
- **Soft Deletes:** This table does not appear to implement a soft-delete flag; assume standard CRUD operations.
- **Data Sensitivity:** Contains no direct PII, though it links to resource/employee IDs which may be sensitive in downstream reporting.