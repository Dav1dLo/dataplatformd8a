# resource_calendar_leaves

## Source system
This table originates from an Odoo ERP system. The naming convention (e.g., `resource_calendar_leaves`, `create_uid`, `write_date`) and the specific sequence-based primary key pattern are characteristic of Odoo's ORM-generated database schema.

## Functional process 
This table supports the Human Resources and Scheduling modules, specifically tracking time-off, leave requests, or calendar exceptions for specific resources. It links resources to calendars to define periods where the resource is unavailable or assigned to a specific non-standard activity type.

## Description
One row represents a single leave period or calendar exception for a specific resource, defined by a start and end timestamp. As a staging table, this represents a raw, landed copy of the Odoo `resource.calendar.leaves` model, intended for use in downstream transformations to calculate resource availability or payroll-related leave accruals.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| company_id | INTEGER | true | Foreign key to the company | Identifies the organization owning this record. |
| calendar_id | INTEGER | true | Foreign key to the calendar | Links the leave to a specific resource calendar. |
| resource_id | INTEGER | true | Foreign key to the resource | The employee or equipment associated with the leave. |
| create_uid | INTEGER | true | Creator user ID | ID of the user who created the record. |
| write_uid | INTEGER | true | Last modifier user ID | ID of the user who last updated the record. |
| name | VARCHAR | true | Leave description | A label or reason for the leave entry. |
| time_type | VARCHAR | true | Type of time entry | Categorizes the leave (e.g., 'leave', 'other'). |
| date_from | TIMESTAMP | false | Start timestamp | Beginning of the leave period. |
| date_to | TIMESTAMP | false | End timestamp | Conclusion of the leave period. |
| create_date | TIMESTAMP | true | Creation timestamp | Audit timestamp for record creation. |
| write_date | TIMESTAMP | true | Last update timestamp | Audit timestamp for the last modification. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `company_id` → `res_company.id` (Standard Odoo multi-company architecture).
    - `calendar_id` → `resource_calendar.id` (Links to the calendar definition).
    - `resource_id` → `resource_resource.id` (Links to the specific resource entity).
    - `create_uid` / `write_uid` → `res_users.id` (Standard Odoo user tracking).
- **Natural keys (inferred):** Not confidently inferable. Odoo typically relies on the surrogate `id` for uniqueness.

## Caveats for downstream consumers

- **Sensitive Data:** Contains user-identifiable information via `create_uid` and `write_uid`, and potentially sensitive leave reasons in the `name` column.
- **Timezones:** Timestamps are typically stored in UTC in Odoo; verify against the `res_company` timezone settings if local time conversion is required.
- **Soft Deletes:** This table does not appear to implement a soft-delete flag; records are generally hard-deleted in Odoo unless an `active` column is present (which is absent here).
- **Data Integrity:** `date_from` and `date_to` are mandatory, but ensure logic handles cases where `date_to` < `date_from` due to data entry errors.