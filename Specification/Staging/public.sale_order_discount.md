# sale_order_discount

## Source system
This table originates from an Odoo ERP system. The naming convention (e.g., `sale_order_id`, `create_uid`, `write_uid`, `write_date`) and the use of PostgreSQL sequence-based defaults are characteristic of Odoo's ORM-generated database schema.

## Functional process 
This table supports the sales order management process, specifically tracking line-item or order-level discounts applied to sales transactions. It captures the financial adjustments made to orders, allowing for the calculation of net order values and the auditing of discount application history via the `create_uid` and `write_uid` audit columns.

## Description
One row represents a single discount record associated with a specific sales order. It serves as a raw landed copy of the discount data from the source ERP, maintaining the grain of individual discount applications per order.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by source. |
| sale_order_id | INTEGER | false | Foreign key to the parent sales order | Links to the primary sales order record. |
| create_uid | INTEGER | true | ID of the user who created the record | References the system user table. |
| write_uid | INTEGER | true | ID of the user who last modified the record | References the system user table. |
| discount_type | VARCHAR | true | Categorization of the discount | e.g., 'fixed', 'percentage', 'global'. |
| discount_amount | NUMERIC | true | Fixed monetary value of the discount | Precision/scale not specified; assume currency units. |
| create_date | TIMESTAMP | true | Record creation timestamp | Inferred UTC based on Odoo standards. |
| write_date | TIMESTAMP | true | Last modification timestamp | Inferred UTC based on Odoo standards. |
| discount_percentage | DOUBLE PRECISION | true | Percentage value of the discount | Used when `discount_type` is 'percentage'. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `sale_order_id` → `sale_order.id` (Inferred from naming convention and standard Odoo schema patterns).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **Sensitive Data:** `create_uid` and `write_uid` link to user identities; ensure these are handled according to internal PII/privacy policies.
- **Timestamps:** Timestamps are assumed to be in UTC, consistent with standard Odoo/PostgreSQL deployments.
- **Data Integrity:** `discount_amount` and `discount_percentage` are both nullable; ensure logic accounts for which field is populated based on the `discount_type`.
- **Soft Deletes:** This table does not explicitly show a soft-delete flag (e.g., `active` or `is_deleted`), so assume all records are active unless otherwise specified by the source system's business logic.