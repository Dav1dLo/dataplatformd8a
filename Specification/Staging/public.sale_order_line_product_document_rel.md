# sale_order_line_product_document_rel

## Source system
This table likely originates from an Odoo or similar ERP system, as indicated by the `_rel` suffix and the naming convention linking two distinct entities (`sale_order_line` and `product_document`). These patterns are characteristic of many-to-many relationship tables generated by ORMs in modular business applications.

## Functional process 
This table supports the document management process within the sales pipeline. It maps specific product-related documentation (such as manuals, safety sheets, or certificates) to individual line items within a sales order, ensuring that the correct collateral is associated with the items being sold.

## Description
One row in this table represents a single association between a sales order line item and a product document. It serves as a raw landing join table in the staging layer, facilitating the resolution of a many-to-many relationship between sales orders and product documentation.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| sale_order_line_id | INTEGER | false | Foreign key to the sales order line | Links to the primary key of the sales order line table. |
| product_document_id | INTEGER | false | Foreign key to the product document | Links to the primary key of the product document table. |

## Keys

- **Primary key (inferred):** The combination of `(sale_order_line_id, product_document_id)` is the inferred composite primary key.
- **Foreign keys (inferred):** 
    - `sale_order_line_id` → `sale_order_line.id`: Guessed based on the column name and standard relational naming conventions.
    - `product_document_id` → `product_document.id`: Guessed based on the column name and standard relational naming conventions.
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- This table is a pure join table; it contains no descriptive attributes, only identifiers.
- There are no timestamps or audit columns present; it is impossible to determine the creation or modification time of these relationships from this table alone.
- Ensure that joins to the parent tables (`sale_order_line` and `product_document`) handle potential orphans if referential integrity is not strictly enforced in the source system.