# sms_template

## Source system
This table originates from an Odoo ERP system, as evidenced by the naming convention of columns like `create_uid`, `write_uid`, `create_date`, and `write_date`, which are standard audit fields in the Odoo framework, as well as the use of `JSONB` for translatable fields.

## Functional process 
This table supports the automated communication and notification engine. It stores templates used for sending SMS messages, likely triggered by specific business events or workflows defined by the `model` and `model_id` references.

## Description
Each row represents a single SMS message template configured for a specific business object or module. This table serves as a raw landing copy of the Odoo `sms.template` model, capturing the template content, language settings, and associated metadata.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by `sms_template_id_seq`. |
| model_id | INTEGER | false | Foreign key to the related business model | Links to the Odoo model definition. |
| sidebar_action_id | INTEGER | true | Reference to a UI action | Links to the sidebar action configuration. |
| create_uid | INTEGER | true | User ID who created the record | References `res.users`. |
| write_uid | INTEGER | true | User ID who last updated the record | References `res.users`. |
| template_fs | VARCHAR | true | Filesystem template path | Used if the template is stored as a file. |
| lang | VARCHAR | true | Language code | ISO language code (e.g., 'en_US'). |
| model | VARCHAR | true | Technical name of the model | The Odoo model name (e.g., 'res.partner'). |
| name | JSONB | true | Template name | Stored as JSONB for multi-language support. |
| body | JSONB | false | SMS message content | The template body, potentially containing placeholders. |
| create_date | TIMESTAMP | true | Record creation timestamp | Assumed UTC. |
| write_date | TIMESTAMP | true | Record last update timestamp | Assumed UTC. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Guess: standard Odoo audit field).
    - `write_uid` → `res_users.id` (Guess: standard Odoo audit field).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **JSONB Content:** The `name` and `body` columns contain `JSONB` data. Downstream consumers will need to use PostgreSQL JSON operators (e.g., `->>`) to extract text values.
- **Timestamps:** All `create_date` and `write_date` fields are assumed to be in UTC, consistent with standard Odoo database configurations.
- **Soft Deletes:** This table does not appear to implement a soft-delete flag; assume all records are active unless otherwise specified by the source system logic.
- **PII:** While this table contains templates, ensure that the `body` column is audited for any hardcoded PII or sensitive data that might have been inadvertently included in template definitions.