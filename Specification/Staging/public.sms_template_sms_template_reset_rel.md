# sms_template_sms_template_reset_rel

## Source system
The table likely originates from a relational database supporting a custom application or a CMS (e.g., Django or a similar ORM-based framework), given the naming convention `_rel` which is characteristic of many-to-many join tables generated by ORMs.

## Functional process 
This table supports the association between SMS templates and reset-related configurations or triggers. It facilitates a many-to-many relationship where specific SMS templates are linked to specific reset workflows or reset-related entities.

## Description
One row in this table represents a single association between an SMS template and a reset configuration. It serves as a raw landing join table in the staging layer, maintaining the referential link between the two entities to allow for normalized querying of template-to-reset mappings.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| sms_template_reset_id | INTEGER | false | Foreign key to the reset configuration entity | Represents the identifier for the reset-related record. |
| sms_template_id | INTEGER | false | Foreign key to the SMS template entity | Represents the identifier for the SMS template record. |

## Keys

- **Primary key (inferred):** Not confidently inferable. While this is a join table, it lacks a surrogate primary key; it likely relies on a composite primary key of `(sms_template_reset_id, sms_template_id)`.
- **Foreign keys (inferred):** 
    - `sms_template_reset_id` → `sms_template_reset.id` (Guess: based on standard naming conventions for join tables).
    - `sms_template_id` → `sms_template.id` (Guess: based on standard naming conventions for join tables).
- **Natural keys (inferred):** The combination of `(sms_template_reset_id, sms_template_id)` acts as the natural business key for this relationship.

## Caveats for downstream consumers

- This table is a junction table; ensure joins to parent tables are handled correctly to avoid fan-out issues.
- There are no timestamps or soft-delete flags present; assume this table reflects the current state of associations as captured during the last ingestion.
- The table contains no PII or sensitive data.