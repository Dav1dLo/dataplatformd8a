# snailmail_letter_format_error

## Source system
This table originates from an Odoo ERP system. The naming convention (e.g., `create_uid`, `write_uid`, `create_date`, `write_date`) and the use of PostgreSQL sequence-based primary keys are characteristic of the Odoo framework's ORM layer.

## Functional process 
This table supports the "Snailmail" (physical mail) communication module within the ERP. It tracks errors encountered during the formatting or processing of physical letters, likely linking to a parent message entity to provide auditability for failed postal delivery attempts.

## Description
One row represents a single formatting error event associated with a physical mail dispatch attempt. It serves as a raw staging record capturing the metadata and audit trail of why a specific letter failed to process correctly.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by the source system. |
| message_id | INTEGER | true | Foreign key to the parent message | Links to the communication record that failed. |
| create_uid | INTEGER | true | Creator user ID | ID of the user who triggered the error record creation. |
| write_uid | INTEGER | true | Last modifier user ID | ID of the user who last updated this record. |
| snailmail_cover | BOOLEAN | true | Cover page flag | Indicates if the error relates to a snailmail cover page. |
| create_date | TIMESTAMP | true | Record creation timestamp | Timestamp of when the error was logged. |
| write_date | TIMESTAMP | true | Last update timestamp | Timestamp of the last modification to this record. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `message_id` → `mail_message.id` (Guess: standard Odoo pattern for linking error logs to messages).
    - `create_uid` → `res_users.id` (Guess: standard Odoo audit field).
    - `write_uid` → `res_users.id` (Guess: standard Odoo audit field).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **Sensitive Data:** Contains `create_uid` and `write_uid`, which link to internal user identities.
- **Timestamps:** Assumed to be in UTC, consistent with standard Odoo PostgreSQL deployments.
- **Soft Deletes:** This table does not appear to implement a soft-delete flag (e.g., `active`).
- **Data Integrity:** `message_id` is nullable, which may imply some error logs are orphaned or system-generated without a direct message link.