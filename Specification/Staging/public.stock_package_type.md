# stock_package_type

## Source system
This table originates from an Odoo ERP system. The naming convention (e.g., `create_uid`, `write_uid`, `write_date`) and the presence of a `sequence` column are characteristic patterns of the Odoo ORM framework used for managing inventory and logistics master data.

## Functional process 
This table supports the inventory management and logistics process by defining standard packaging types used for shipping and storage. It allows the system to calculate volumetric weight and physical constraints for stock movements, linking specific dimensions and weight capacities to package identifiers.

## Description
One row in this table represents a single defined packaging configuration (e.g., "Box", "Pallet", "Envelope") available for use within the warehouse. This is a raw landed staging table containing the master configuration for package dimensions and weight limits, intended for use in downstream logistics and shipping calculations.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| sequence | INTEGER | true | Display order index | Used for UI sorting. |
| company_id | INTEGER | true | Owning company ID | Links to the multi-company configuration. |
| create_uid | INTEGER | true | Creator user ID | References the user who created the record. |
| write_uid | INTEGER | true | Last modifier user ID | References the user who last updated the record. |
| name | VARCHAR | false | Package type name | Human-readable label. |
| barcode | VARCHAR | true | Barcode identifier | Used for scanning during packing operations. |
| create_date | TIMESTAMP | true | Creation timestamp | Recorded in UTC by the application. |
| write_date | TIMESTAMP | true | Last update timestamp | Recorded in UTC by the application. |
| height | DOUBLE PRECISION | true | Package height | Unit of measure depends on system settings (usually cm/mm). |
| width | DOUBLE PRECISION | true | Package width | Unit of measure depends on system settings (usually cm/mm). |
| packaging_length | DOUBLE PRECISION | true | Package length | Unit of measure depends on system settings (usually cm/mm). |
| base_weight | DOUBLE PRECISION | true | Empty package weight | Weight of the packaging material itself. |
| max_weight | DOUBLE PRECISION | true | Maximum load capacity | Maximum weight the package can safely hold. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `company_id` → `res_company.id` (Guess: standard Odoo multi-company link).
    - `create_uid` → `res_users.id` (Guess: standard Odoo audit trail).
    - `write_uid` → `res_users.id` (Guess: standard Odoo audit trail).
- **Natural keys (inferred):** 
    - `name` (Assuming unique constraint within a company context).

## Caveats for downstream consumers

- **Timestamps:** All `create_date` and `write_date` values are stored in UTC.
- **Units of Measure:** This table does not explicitly store the unit of measure (e.g., cm vs inches, kg vs lbs). Consumers must verify the `res_company` or global system configuration settings to interpret dimensions and weights correctly.
- **Data Integrity:** As a staging table, this may contain records that have been logically deleted or archived in the source system if the Odoo instance does not use hard deletes.
- **Precision:** `DOUBLE PRECISION` is used for physical measurements; ensure downstream casting accounts for potential floating-point rounding issues.