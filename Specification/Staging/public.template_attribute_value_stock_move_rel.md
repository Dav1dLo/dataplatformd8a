# template_attribute_value_stock_move_rel

## Source system
This table likely originates from an Odoo ERP or a similar modular Python-based framework. The naming convention `_rel` is characteristic of many-to-many join tables generated by ORMs (like SQLAlchemy or Odoo's ORM) to link stock movement records with specific product attribute configurations.

## Functional process 
This table supports the inventory management and product configuration process. It maps specific product attribute values (e.g., "Color: Blue", "Size: XL") to individual stock move records, allowing the system to track inventory movements at the granular level of product variants.

## Description
Each row represents a single association between a stock movement record and a specific product attribute value. It acts as a junction table in the staging layer, providing the raw link required to reconstruct product variant details for inventory reporting.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| move_id | INTEGER | false | Foreign key to the stock move record. | Links to the primary stock movement entity. |
| template_attribute_value_id | INTEGER | false | Foreign key to the product attribute value. | Identifies the specific variant attribute applied to this move. |

## Keys

- **Primary key (inferred):** Not confidently inferable; likely a composite primary key on (`move_id`, `template_attribute_value_id`).
- **Foreign keys (inferred):** 
    - `move_id` → `stock_move.id` (Inferred from naming convention).
    - `template_attribute_value_id` → `product_template_attribute_value.id` (Inferred from naming convention).
- **Natural keys (inferred):** The combination of (`move_id`, `template_attribute_value_id`) acts as the natural business key for this relationship.

## Caveats for downstream consumers

- This is a junction table; expect no descriptive attributes other than the two foreign keys.
- Ensure inner joins are used when filtering for specific attribute-move combinations to avoid orphaned records.
- There is no audit timestamp or soft-delete flag present; assume this table reflects the current state of relationships as captured during the last ingestion.